<%= form_for(food_risk) do |f| %>
  <% if food_risk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_risk.errors.count, "error") %> prohibited this food_risk from being saved:</h2>

      <ul>
      <% food_risk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
